import { IConfig } from "./core/http/config";

export const urlPath = "gin-demo-server";

// 项目基本变量配置
const appConfig: IConfig = {
  appName: "webapp",
  baseURL: "https://srv-gin-demo-server---devops.devops-jiahuayun-dev.rockontrol.com",
  version: "",
  env: "demo",
};

export default appConfig;
